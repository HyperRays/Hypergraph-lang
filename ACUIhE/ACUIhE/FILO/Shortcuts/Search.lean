import ACUIhE.FILO.Shortcuts.Engine

/-!
# FILO shortcut search

The choice-specific search is `Shortcuts.Engine.search`. Its table stores
acyclic resolver edges and stops on initial resolution or a fixed point.
The public choice loop and implicit solver are in `FILO.GenericSolver`.
-/
