import HypergraphML

/-!
This executable is never distributed. Lake uses its precise import closure to
produce the native-object response file consumed by the standalone packager.
-/

def main : IO Unit := pure ()

